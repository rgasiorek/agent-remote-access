# Authentication credentials for accessing the web interface
AUTH_USERNAME=your_username
AUTH_PASSWORD=your_secure_password

# Authentication: Run 'claude login' in your terminal before starting the servers
# The system will use the authentication session from ~/.claude.json

# Email notification settings (optional - for tunnel URL notifications)
# Leave EMAIL_TO empty to disable email notifications
EMAIL_TO=
EMAIL_FROM=agent-remote-access@example.com
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASSWORD=

# Path to the Claude Code project you want to interact with
# Leave empty - it will be set automatically to the directory where you run start.sh from
# This allows you to run the server from any project directory
CLAUDE_PROJECT_PATH=

# Server configuration
# For Docker: HOST is set to 0.0.0.0 automatically in docker-compose
# For local: Use 127.0.0.1
HOST=127.0.0.1
PORT=8000
