# Authentication credentials
AUTH_USERNAME=your_username
AUTH_PASSWORD=your_secure_password

# Path to the Claude Code project you want to interact with
# For Docker: Absolute path on your HOST machine (will be mounted to /workspace in container)
# For local: Leave empty to use the current directory
# Example: /Users/yourname/projects/my-project
CLAUDE_PROJECT_PATH=

# Server configuration
# For Docker: HOST must be 0.0.0.0 (set automatically in docker-compose)
# For local: Use 127.0.0.1
HOST=127.0.0.1
PORT=8000
