# Authentication credentials for accessing the web interface
AUTH_USERNAME=your_username
AUTH_PASSWORD=your_secure_password

# Anthropic API key - NOT NEEDED if you've run 'claude login'
# The host bridge uses authentication from ~/.claude.json
# Only set this if you want to override the default auth
# ANTHROPIC_API_KEY=

# Path to the Claude Code project you want to interact with
# Docker will mount this path so Claude CLI can access your project files
# Leave empty to use the current directory (where docker-compose.yml is)
# Example: /Users/yourname/projects/my-project
CLAUDE_PROJECT_PATH=

# Sessions storage path (Docker only)
# Where to store conversation sessions on your host machine
# Default: ./sessions (relative to docker-compose.yml)
# You can change this to any absolute path for backups
# Example: /Users/yourname/claude-sessions or /mnt/backup/sessions
SESSIONS_PATH=./sessions

# Server configuration
# For Docker: HOST is set to 0.0.0.0 automatically in docker-compose
# For local: Use 127.0.0.1
HOST=127.0.0.1
PORT=8000
