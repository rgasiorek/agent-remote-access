# Authentication credentials for accessing the web interface
AUTH_USERNAME=your_username
AUTH_PASSWORD=your_secure_password

# Anthropic API key for Claude Code CLI
# Get it from: https://console.anthropic.com/settings/keys
# Required for Docker (CLI authenticates via env var)
# Optional for local (if already authenticated with `claude login`)
ANTHROPIC_API_KEY=

# Path to the Claude Code project you want to interact with
# For Docker: Not used - container uses internal /workspace
# For local: Leave empty to use the current directory
# Example: /Users/yourname/projects/my-project
CLAUDE_PROJECT_PATH=

# Server configuration
# For Docker: HOST is set to 0.0.0.0 automatically in docker-compose
# For local: Use 127.0.0.1
HOST=127.0.0.1
PORT=8000
