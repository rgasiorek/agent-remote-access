# Nginx configuration for Claude Code Remote Access
# Single entry point that routes requests to backend services

events {
    worker_connections 1024;
}

http {
    # Define mime types inline (no external dependencies)
    types {
        text/html                             html htm;
        text/css                              css;
        application/javascript                js;
        application/json                      json;
        image/png                             png;
        image/jpeg                            jpg jpeg;
        image/gif                             gif;
        image/svg+xml                         svg;
    }
    default_type application/octet-stream;

    # Logging
    access_log logs/nginx-access.log;
    error_log logs/nginx-error.log;

    # Timeout settings for long-running AI requests
    # Set to 10 minutes to handle complex tasks
    proxy_connect_timeout 600s;
    proxy_send_timeout 600s;
    proxy_read_timeout 600s;
    send_timeout 600s;

    # Main server block
    server {
        listen 80;
        server_name localhost 127.0.0.1;

        # Health check endpoint
        location /health {
            return 200 '{"status":"healthy","service":"nginx-gateway"}';
            add_header Content-Type application/json;
        }

        # Route /api/* to agent-api backend (port 8001)
        location /api/ {
            proxy_pass http://127.0.0.1:8001;
            proxy_http_version 1.1;

            # Forward headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # WebSocket support (if needed in future)
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";

            # Timeouts for long-running AI requests (10 minutes)
            proxy_connect_timeout 600s;
            proxy_send_timeout 600s;
            proxy_read_timeout 600s;
        }

        # Route everything else to portal-ui (port 8000)
        location / {
            proxy_pass http://127.0.0.1:8000;
            proxy_http_version 1.1;

            # Forward headers
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;

            # Cache control for static assets
            proxy_set_header Cache-Control "no-cache, no-store, must-revalidate";
        }
    }
}
